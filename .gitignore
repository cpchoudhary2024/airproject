# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Tool / build caches (regenerable — see clean.sh)
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.matplotlib/

# Virtual environments
venv/
.venv/
env/

# Environment variables
.env
.env.local

# Runtime data (uploaded files, generated outputs)
app/data/
test_output/
test_output_v2/
test_output_v3/
test_output_fix/
test_output_refactor/
test_stl_check/
test_stl_fix/

# Generated PDF reports
report*.pdf
report_*.pdf

# Raw data files (large binary/csv - not needed in repo)
*.xlsx
0-Minute*.csv
# PurpleAir raw exports (e.g. "291796 2026-02-20 ... 0-Minute Average.csv")
*0-Minute Average.csv
*Average.csv
purple airs raw data*/
air reports community*/

# Local report-generation scripts, evidence, and generated assets (not part of the deployed app)
build_reports.py
compute_and_plot.py
evidence.json
build_reports_docx.py
report_assets/
COMPARISON_ANALYSIS_REPORT.md
Welcome file*.md
*.pdf

# Claude working docs
CLAUDE.md
CLAUDE copy.md

# VS Code
.vscode/

# macOS
.DS_Store

# Vercel (no longer used)
.vercel
vercel.json
api/

# Internal project docs (not needed for deployment)
ACTION_PLAN.md
ARCHITECTURE_DIAGRAM.md
DOCUMENTATION_INDEX.md
QUICK_START.md
BEFORE_AFTER_COMPARISON.md
IMPROVEMENTS_IMPLEMENTED.md
PROFESSIONAL_ENHANCEMENTS.md
PROJECT_SUMMARY.md
RESTORATION_SUMMARY.md
quickstart.sh

# Generated evidence reports (binary; kept out of git — HF rejects non-LFS binaries)
Report_*.pdf
Report_*.docx
Community_Air_Quality_Report*.docx
Defense_Notes_Methods.docx
build_defense_notes.py

# ─── Data-engineering hygiene (added by audit) ───
# Secrets — never commit credentials
.env
.env.*
!.env.example

# Raw / bulk data: keep on disk, keep out of git (regenerate or fetch on demand)
data/raw/*
data/cache/
*.nc
*.nc4
*.hdf5
*.parquet
*.zip
*.7z

# Build artifacts & dependency trees
node_modules/
dist/
build/
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.coverage
htmlcov/

# Stray logs & run debris
*.log
logs/
*.pid

# Office lock/temp files (e.g. ~$report.docx)
~$*
*.tmp

# OS noise
.DS_Store
Thumbs.db

# PurpleAir raw sensor exports (bulk, PII-bearing directory names)
purple airs raw data*/
*0-Minute Average.csv
*0-Minute Average.xlsx

# ─── Keep the standard scaffold under version control ───
!data/raw/.gitkeep
!data/processed/.gitkeep
!outputs/
!outputs/plots/
!outputs/plots/.gitkeep
!notebooks/

# ─── PII GUARD — participant identities (never publish) ───
# Embeds resident names/addresses for per-household report generation.
# Verified untracked and absent from git history at the time this was added.
build_resident_air_report.py
Resident*Report*.docx
resident_report_assets/
# Raw sensor exports are foldered by resident first name.
purple airs raw data*/
